{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug DCA tests",
      "cargo": {
        "args": ["test", "--no-run", "--lib", "--package=dca"],
        "filter": {
          "name": "dca",
          "kind": "lib"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
